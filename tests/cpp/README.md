# C++ Tests Directory

This directory contains C++ unit tests for the HNSW implementation.

## Files (to be created by hnsw_specialist agent)

- `test_hnsw.cpp` - HNSW unit tests

## Running Tests

C++ tests are primarily run through Python bindings.
See `tests/python/test_hnsw_binding.py` for binding tests.
